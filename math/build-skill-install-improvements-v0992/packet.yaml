task_id: build-skill-install-improvements-v0992
title: build-skill + install-skill pipeline improvements
lifecycle: applied
implementation: complete
substrate: shell
rigor: light
decision: made
created: "2026-07-23"
creator: za
verified_by: [za, agent]
axiom: false
single_author: true
# stable_since: opt-in. Run 'math-coding stable <name>' to set.
stable_since: null
verifier: sh math-coding probe
depends_on:
  - 06-self-application
  - multi-agent-skills-v0992
applications: []
surface_impact: |
  meta/build-skill.sh (refactor: helper, mktemp, if/then parser, sanity check),
  core/install/install-skill.sh (hooks warning for non-opencode agents)
reviews:
  - by: za
    date: "2026-07-23"
    verdict: approve
    note: |
      Regression: opencode SKILL.md + math-agent.md byte-identical
      (cmp verified). Refactor addresses code-review recommendations
      1, 3, 4, 5, 11, 16. ensure_trailing_newline helper uses
      od -tx1 (portable). gen_block writes to mktemp file
      (avoids awk quoting issues). sanity check between AGENTS=
      and install-skill.sh AGENT_TARGETS= warns on drift.
      install-skill.sh hooks skipped for non-opencode agents.
