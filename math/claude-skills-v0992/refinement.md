# Refinement: claude-skills-v0992

## State

- pre:  extensions/agents/claude/ did not exist. install-skill.sh --agent=claude had its TARGET_PATH defined (~/.claude/skills) but no SKILL.md to copy and the agent install was hardcoded to ~/.config/opencode/agents/.
- post: extensions/agents/claude/{SKILL.preamble.md, SKILL.md} exist. SKILL.preamble.md has Anthropic Skills-compatible frontmatter (name + description, no license/metadata). SKILL.md generated by build-skill.sh, body identical to opencode (canon-derived). install-skill.sh --agent=claude copies SKILL.md to ~/.claude/skills/math-coding/ and skips agent install (note printed).

## Operation

  1. Write extensions/agents/claude/SKILL.preamble.md with Anthropic Skills frontmatter (`name: math-coding`, `description: ...`).
  2. Add "claude" to AGENTS= in meta/build-skill.sh.
  3. Update install-skill.sh: agent install path becomes a per-agent case statement. Only opencode installs agents in v0.992; claude (and future agents without subagent support) print a note and skip.
  4. Run sh meta/build-skill.sh claude — generates extensions/agents/claude/SKILL.md.
  5. Run sh math-coding install-skill --dry-run --agent=claude — verify target and source paths.
  6. Run sh math-coding probe — expect 0 errors.

## Invariant preservation

  claude SKILL.md has frontmatter with name + description only (Anthropic Skills spec). Body identical to opencode (extracted from canon/SKILL.body.template.md). axiom A6 holds: math-coding applies to itself.

## Mapping (spec → impl)

  spec:  claude SKILL.md has Anthropic Skills frontmatter
  impl:  extensions/agents/claude/SKILL.preamble.md lines 1-4 (YAML frontmatter)

  spec:  install-skill.sh --agent=claude installs SKILL only
  impl:  core/install/install-skill.sh lines 197-227 (case $AGENT; opencode only)

## Test obligation

  1. `sh math-coding probe` exits 0 in source-repo mode.
  2. `sh meta/build-skill.sh claude --check` exits 0.
  3. `sh meta/build-skill.sh --list` includes "claude".
  4. `sh math-coding install-skill --dry-run --agent=claude` prints `~/.claude/skills/math-coding` target and SKILL.md only.
  5. `head -1 extensions/agents/claude/SKILL.md` is `---` (frontmatter start).
  6. claude SKILL.md body matches opencode SKILL.md body (diff after stripping frontmatter).
