---
proposition: "  meta/build-skill.sh generates universal/AGENTS.md from canon/AGENTS.body.template.md + universal/AGENTS.preamble.md. AGENTS.md was hand-written in v0.992 (universal-agents-md-v0992); now generated, axiom cards pulled from core/spec/axioms.md at build time. Drift risk eliminated. "
antithesis: "  AGENTS.md was a 63-line static file; generating it adds build complexity for limited value (axioms change infrequently). "
synthesis: "  Build pipeline eliminates drift risk: axiom changes in canon propagate to AGENTS.md. Conditional preamble detection (SKILL vs AGENTS vs math-agent) keeps the pipeline flexible per agent. universal added to ALL_AGENTS but excluded from SKILL_AGENTS sanity check (no install path). "
axiom: false
substrate: none
---

# universal-build-pipeline-v0992

## Thesis

  meta/build-skill.sh generates universal/AGENTS.md from canon/AGENTS.body.template.md + universal/AGENTS.preamble.md. AGENTS.md was hand-written in v0.992 (universal-agents-md-v0992); now generated, axiom cards pulled from core/spec/axioms.md at build time. Drift risk eliminated.

## Antithesis

  AGENTS.md was a 63-line static file; generating it adds build complexity for limited value (axioms change infrequently).

## Surface impact

  - extensions/agents/canon/AGENTS.body.template.md (new)
  - extensions/agents/universal/AGENTS.preamble.md (new)
  - extensions/agents/universal/AGENTS.md (regenerated from canon)
  - meta/build-skill.sh (gen_block_agents_to_file, process_agents_body, build_agents_md_for; SKILL_AGENTS / ALL_AGENTS / AGENTS vars)

## Synthesis

  Build pipeline eliminates drift risk: axiom changes in canon propagate to AGENTS.md. Conditional preamble detection (SKILL vs AGENTS vs math-agent) keeps the pipeline flexible per agent. universal added to ALL_AGENTS but excluded from SKILL_AGENTS sanity check (no install path).

## Worked example

  When an axiom changes in core/spec/axioms.md:

  ```sh
  $ sh meta/build-skill.sh universal
  wrote: /path/to/extensions/agents/universal/AGENTS.md
  $ sh meta/build-skill.sh universal --check
  ok: .../universal/AGENTS.md up-to-date
  ```

  No hand-editing required. Axiom cards flow from canon.

## Proof

  - sh math-coding probe → 0 errors, axiom A6 PROVEN
  - sh meta/build-skill.sh universal builds AGENTS.md with axiom cards
  - sh meta/build-skill.sh universal --check → ok
  - sh meta/build-skill.sh --list → "opencode claude cursor universal"
  - regression: opencode/claude/cursor --check all pass

# Refinement: universal-build-pipeline-v0992

## State

- pre:  extensions/agents/universal/AGENTS.md was hand-written (63 lines). Axiom section was static text. Any axiom change in core/spec/axioms.md required manual sync to AGENTS.md (drift risk).
- post: extensions/agents/universal/AGENTS.md generated by build-skill.sh from canon/AGENTS.body.template.md + universal/AGENTS.preamble.md. Axiom cards flow from core/spec/axioms.md at build time. Drift eliminated.

## Operation

  1. Create extensions/agents/canon/AGENTS.body.template.md — body content with BEGIN/END GENERATED markers around the axiom section.
  2. Create extensions/agents/universal/AGENTS.preamble.md — title + intro.
  3. Add to meta/build-skill.sh:
     - SKILL_AGENTS=opencode claude cursor
     - ALL_AGENTS=$SKILL_AGENTS universal
     - AGENTS=$ALL_AGENTS
     - gen_block_agents_to_file(out) — emits axiom cards block
     - process_agents_body() — replaces BEGIN/END region in canon body
     - build_agents_md_for(agent) — assembles AGENTS.md from preamble + body
  4. Update main loop: per-agent preamble detection (SKILL.preamble.md, math-agent.preamble.yaml, AGENTS.preamble.md). Skip artifacts whose preamble doesn't exist.
  5. Update sanity check: use SKILL_AGENTS (not AGENTS) since universal has no install path.

## Invariant preservation

  SKILL_AGENTS=opencode claude cursor (user-skill path). universal listed separately in ALL_AGENTS, no AGENT_TARGETS check (universal is project-local). axiom A6 holds. Per-agent preamble detection.

## Mapping (spec → impl)

  spec:  universal/AGENTS.md generated
  impl:  meta/build-skill.sh build_agents_md_for function

  spec:  axiom cards flow from canon
  impl:  canon/AGENTS.body.template.md + gen_block_agents_to_file (uses emit_axiom_cards)

  spec:  per-agent preamble detection
  impl:  main loop checks for SKILL.preamble.md / AGENTS.preamble.md / math-agent.preamble.yaml

## Test obligation

  1. `sh math-coding probe` exits 0.
  2. `sh meta/build-skill.sh universal` writes AGENTS.md.
  3. `sh meta/build-skill.sh universal --check` exits 0.
  4. `sh meta/build-skill.sh --list` includes "universal".
  5. `sh meta/build-skill.sh opencode --check` exits 0 (regression).
  6. AGENTS.md contains axiom cards from core/spec/axioms.md.
