task_id: universal-build-pipeline-v0992
assumptions:
  - id: A1
    statement: "AGENTS.md axiom section can be regenerated from canon without losing context"
    status: agent-inferred
    epistemology: fact
    confidence: 0.95
    evidence: |
      emit_axiom_cards reads core/spec/axioms.md and emits heading + statement for each axiom. Generated content matches the hand-written section's structure.
  - id: A2
    statement: "universal added to ALL_AGENTS but excluded from SKILL_AGENTS sanity check"
    status: agent-inferred
    epistemology: judgment
    confidence: 0.9
    evidence: |
      universal has no install path (project-local). The sanity check warns on SKILL_AGENTS drift from install-skill.sh AGENT_TARGETS; including universal would cause a false-positive warning.
  - id: A3
    statement: "Per-agent preamble detection handles SKILL/AGENTS/math-agent independently"
    status: agent-inferred
    epistemology: fact
    confidence: 0.95
    evidence: |
      Each agent dir has zero or more preamble files (SKILL.preamble.md, AGENTS.preamble.md, math-agent.preamble.yaml). Main loop checks for file existence before invoking the corresponding build function.
  - id: A4
    statement: "AGENTS.md output is plain markdown without YAML frontmatter"
    status: agent-inferred
    epistemology: fact
    confidence: 0.95
    evidence: |
      Codex CLI / GitHub Copilot / Continue / Cursor 1+ read AGENTS.md as plain markdown. YAML frontmatter is not part of the AGENTS.md convention.
  - id: A5
    statement: "AGENTS.md body has fewer sections than SKILL.md (no FSM, no commands)"
    status: agent-inferred
    epistemology: judgment
    confidence: 0.9
    evidence: |
      AGENTS.md is a 60-line condensed fallback. SKILL.md is 16KB with full FSM, Commands, Worked examples. AGENTS.md only needs axioms and basic workflow.
